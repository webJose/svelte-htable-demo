<p>
    Fancy tricks aside, the <code>Htable</code> component is quite simple:  It's just a table that hosts other tables.
</p>
<p>
    This example shows 2 things:  How we can specify sub-tables to be shown by default, and how to detect when the end 
    user expands or collapses sub tables, which may or may not help your devious purposes. 😒
</p>